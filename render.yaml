services:
  - name: web
    buildCommand: npm run build
    startCommand: npm start
    environment:
      - PORT: 3000
    healthCheckPath: /

buildCommand: npm install --legacy-peer-deps
publishDir: public
