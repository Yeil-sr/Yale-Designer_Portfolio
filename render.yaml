services:
  - name: web
    buildCommand: npm install
    startCommand: npm start
    environment:
      - PORT: 3000
    healthCheckPath: /

buildCommand: npm install --legacy-peer-deps
publishDir: public
